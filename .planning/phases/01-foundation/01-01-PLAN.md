---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cinderx/Jit/jit_rt.h
  - cinderx/Jit/jit_rt.cpp
autonomous: true
requirements:
  - FR-001
must_haves:
  truths:
    - All JITRT_* functions are marked with noinline attribute
    - JITRT_* functions are not inlined by LTO
    - Symbol table shows JITRT functions as external symbols
  artifacts:
    - path: cinderx/Jit/jit_rt.h
      provides: JIT_RUNTIME_API macro and noinline-marked function declarations
      min_lines: 680
      contains: "JIT_RUNTIME_API"
    - path: cinderx/Jit/jit_rt.cpp
      provides: Function implementations with noinline attribute
      contains: "JIT_RUNTIME_API"
  key_links:
    - from: jit_rt.h function declarations
      to: jit_rt.cpp implementations
      via: JIT_RUNTIME_API macro
      pattern: "JIT_RUNTIME_API"
---

<objective>
Protect all JIT runtime functions from LTO inlining by marking them with `__attribute__((noinline, visibility("default")))`.

Purpose: Prevent LTO from inlining JIT runtime helper functions, which causes incorrect code generation and runtime crashes.

Output: jit_rt.h with JIT_RUNTIME_API macro, all JITRT_* functions properly marked.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@cinderx/Jit/jit_rt.h
@cinderx/Jit/jit_rt.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JIT_RUNTIME_API macro to jit_rt.h</name>
  <files>cinderx/Jit/jit_rt.h</files>
  <read_first>
    - cinderx/Jit/jit_rt.h (see current function declarations)
    - cinderx/Common/util.h (check for existing attribute macros)
  </read_first>
  <action>
    Add the following macro definition near the top of jit_rt.h, after the #include lines but before the first function declaration:

    ```cpp
    // JIT Runtime API attribute - prevents LTO from inlining these functions
    #define JIT_RUNTIME_API \
        __attribute__((noinline, visibility("default")))
    ```

    Place it after line 11 (after #include "cinderx/Common/util.h") and before the namespace jit declaration.
  </action>
  <verify>
    <automated>grep -n "JIT_RUNTIME_API" cinderx/Jit/jit_rt.h | head -5</automated>
  </verify>
  <done>JIT_RUNTIME_API macro is defined in jit_rt.h</done>
</task>

<task type="auto">
  <name>Task 2: Mark all JITRT_* function declarations with JIT_RUNTIME_API</name>
  <files>cinderx/Jit/jit_rt.h</files>
  <read_first>
    - cinderx/Jit/jit_rt.h (full file to understand all function declarations)
  </read_first>
  <behavior>
    - All function declarations starting with JITRT_ must be marked
    - The JIT_RUNTIME_API attribute goes before the return type
    - Skip struct definitions (JITRT_StaticCallReturn, JITRT_StaticCallFPReturn)
    - Skip extern variables (JITRT_IterDoneSentinel)
  </behavior>
  <action>
    Add JIT_RUNTIME_API before each JITRT_* function declaration in jit_rt.h.

    Examples of patterns to match:
    - `PyThreadState* JITRT_AllocateAndLinkFrame(...)` → `JIT_RUNTIME_API PyThreadState* JITRT_AllocateAndLinkFrame(...)`
    - `void JITRT_Decref(PyObject* obj)` → `JIT_RUNTIME_API void JITRT_Decref(PyObject* obj)`
    - `PyObject* JITRT_Call(...)` → `JIT_RUNTIME_API PyObject* JITRT_Call(...)`

    Apply to ALL ~100 JITRT_* function declarations in the file.

    Do NOT mark:
    - struct JITRT_StaticCallReturn (lines 23, 28)
    - extern PyObject JITRT_IterDoneSentinel (line 671)
  </action>
  <verify>
    <automated>grep -c "JIT_RUNTIME_API" cinderx/Jit/jit_rt.h && grep -c "^JIT_RUNTIME_API" cinderx/Jit/jit_rt.h</automated>
  </verify>
  <done>All JITRT_* function declarations are marked with JIT_RUNTIME_API (expect ~90-100 occurrences)</done>
</task>

<task type="auto">
  <name>Task 3: Add JIT_RUNTIME_API to function definitions in jit_rt.cpp</name>
  <files>cinderx/Jit/jit_rt.cpp</files>
  <read_first>
    - cinderx/Jit/jit_rt.cpp (function definitions)
    - cinderx/Jit/jit_rt.h (to see the macro definition)
  </read_first>
  <action>
    Add JIT_RUNTIME_API before each JITRT_* function definition in jit_rt.cpp.

    Search for function definitions like:
    - `PyThreadState* JITRT_AllocateAndLinkFrame(`
    - `void JITRT_Decref(`
    - `PyObject* JITRT_Call(`

    And add JIT_RUNTIME_API before the return type.

    Note: Some functions may be defined with `extern "C"` prefix - add JIT_RUNTIME_API after extern "C" if present.
  </action>
  <verify>
    <automated>grep -c "JIT_RUNTIME_API" cinderx/Jit/jit_rt.cpp</automated>
  </verify>
  <done>All JITRT_* function definitions are marked with JIT_RUNTIME_API</done>
</task>

<task type="auto">
  <name>Task 4: Verify compilation passes</name>
  <files>cinderx/Jit/jit_rt.h, cinderx/Jit/jit_rt.cpp</files>
  <read_first>
    - cinderx/Jit/jit_rt.h (verify macro placement)
  </read_first>
  <action>
    Run a quick compilation check to ensure the changes are syntactically correct:

    ```bash
    # Check that the header compiles
    python -c "import cinderx" 2>&1 | head -20
    ```

    Or if build system is available:
    ```bash
    python setup.py build_ext --inplace 2>&1 | tail -30
    ```

    Expected: No compilation errors related to jit_rt.h or jit_rt.cpp
  </action>
  <verify>
    <automated>python -c "import cinderx; print('JIT runtime module loaded successfully')" 2>&1 | grep -E "(successfully|Error|error)" | head -5</automated>
  </verify>
  <done>Code compiles without errors</done>
</task>

</tasks>

<verification>
- [ ] JIT_RUNTIME_API macro exists in jit_rt.h
- [ ] All ~100 JITRT_* function declarations are marked
- [ ] All JITRT_* function definitions are marked
- [ ] Code compiles successfully
- [ ] No syntax errors introduced
</verification>

<success_criteria>
- jit_rt.h contains JIT_RUNTIME_API macro definition
- grep "JIT_RUNTIME_API" jit_rt.h returns ~90-100 lines
- grep "JIT_RUNTIME_API" jit_rt.cpp returns ~90-100 lines
- Python can import cinderx without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
